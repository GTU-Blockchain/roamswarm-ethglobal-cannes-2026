{
  "name": "roam-swarm",
  "private": true,
  "workspaces": [
    "apps/web",
    "contracts",
    "agents/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "swarm": "concurrently --kill-others-on-fail --prefix-colors \"bgMagenta,bgCyan,bgGreen,bgYellow\" --names \"ORCH,LORE,SCOUT,GUIDE\" \"cd agents/orchestrator && npm run dev\" \"cd agents/lore && npm run dev\" \"cd agents/scout && npm run dev\" \"cd agents/guide && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "turbo": "^2.0.0"
  }
}
