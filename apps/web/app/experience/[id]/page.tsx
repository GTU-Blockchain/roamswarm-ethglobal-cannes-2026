'use client';

// TODO: Active experience player
// - Fetch from /api/experience/[id] (agent swarm: history + food + voice)
// - AudioPlayer with stream URL from 0G Storage
// - Story card (History Agent output)
// - Venue recommendation card (Food Agent — Chainlink CRE validated)

export default function ExperiencePage() {
  return <></>;
}
