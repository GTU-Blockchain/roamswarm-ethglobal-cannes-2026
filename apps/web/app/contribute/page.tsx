'use client';

// TODO: Local contributor flow
// - Step 1: World ID 4.0 IDKit verification (VerificationLevel.Orb)
// - Step 2: Form — GPS coordinates, story text, venue tip
// - Submit: call ContributorRegistry.register() on Ethereum Mainnet
// - Success: show ENS subname assigned ({id}.contributors.roam.eth)

export default function ContributePage() {
  return <></>;
}
