// TODO: Main map view
// - Leaflet MapView (dynamic import, no SSR) centered on Cannes
// - POI markers: locked (grey) | free-unlock-available (gold) | owned (colored)
// - GeofenceWatcher triggers UnlockModal at 50m radius
// - Load POIs from data/cannes-pois.json + lock state from UserPOIRegistry contract

export default function MapPage() {
  return <></>;
}
