'use client';

// TODO: User profile
// - Wallet address + ENS subname
// - PointsBalance widget (balance, daily rate, free-unlock available state)
// - Owned POIs list (from UserPOIRegistry contract)
// - City Badges gallery — BadgeCard grid (from CityBadgeNFT contract)

export default function ProfilePage() {
  return <></>;
}
